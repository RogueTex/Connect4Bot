# Optimization Group 2 Project for Connect4

Connect4 bot with CNN/Transformer policy, dataset generation, and Anvil web app. 

Training notebooks live in `training/`, dataset generators in `dataset_generators/`, and EC2/Lightsail + Anvil Uplink deployment in `aws-deploy/`.
